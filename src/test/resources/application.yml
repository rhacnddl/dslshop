spring:
  profiles:
    active: test

  jpa:
    properties:
      hibernate:
        show_sql: false
        format_sql: true
        use_sql_comments: true

logging.level:
  org.hibernate.SQL: debug
